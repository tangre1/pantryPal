# Copy this file to `.env` and populate with your Azure OpenAI credentials.
# Do NOT commit your real `.env` to source control.

AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_DEPLOYMENT=
AZURE_OPENAI_API_VERSION=2024-06-01

# Optional: set a different DB path (defaults to sqlite in project)
# DATABASE_URL=sqlite:///./pantrypal.db
